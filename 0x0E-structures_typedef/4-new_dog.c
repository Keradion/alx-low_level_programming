#include "dog.h"

/**
 * new_dog - function that create a new dog
 * @name: memeber of the struct
 * @age: member of the struct
 * @owner: member of the struct
 *
 * Return: struct
 */

dog_t *new_dog(char *name, float age, char *ownwer)
{

	int len = _strlen(name);
	int len2 = _strlen(owner);

	malloc(sizeof(name)
}
