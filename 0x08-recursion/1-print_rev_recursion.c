#include "main.h"

/**
 * _print_rev_recursion - pintf a string in revers
 * @s: string
 *
 * Return: void
 */
void _print_rev_recursion(char *s)
{
	if (*s != '\0')
	{
	
	}

	else
		_print_rev_recurssion(s++);
}
