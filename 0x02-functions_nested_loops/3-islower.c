#include "main.h"

/**
 * _islower() - check lowercase letters
 * @c: number
 * Return: 1 if lowercase , 0 otherwise
 */

int _islower(int c)
{
if ('a' <= c && c <= 'z')
return (1);
else
return (0);
}

