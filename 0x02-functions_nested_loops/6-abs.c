#include "main.h"
#include <stdio.h>

/**
 * abs : determine the absolute value of a given number
 *@n: number
 * Return: Always 0
 */

int _abs(int n)
{
if (n < 0 || n > 0)
return (1);
else
return (0);
}
